@model MovieWithoutPosterViewModel

 <div>

     <img src="data:image/jpg;base64,@Convert.ToBase64String(TempData["Post"] as byte[])" />
     <h2>@Model.Title</h2>
     <p>@Model.Description</p>
 @*  
            ////////// is null
            <h2>@Model.Genre.Name</h2>
////////////////////////////////////////////////     
 @foreach (var item in Model.Actors)
     {
         <h4>@item.FirstName @item.LastName</h4>
 
     }
 *@
 </div>